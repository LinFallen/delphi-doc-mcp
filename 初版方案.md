下面是将前面方案整理后的「Delphi Doc MCP 架构设计」Markdown 版本，可以直接作为你仓库里的 ARCHITECTURE.md 或 README.md 的核心章节使用。

⸻

Delphi Doc MCP – 架构设计（Architecture）

面向 AI Agent（MCP 协议） 的 Delphi 文档索引与检索服务
覆盖 RAD Studio VCL 与 DevExpress VCL 官方文档
技术栈：Node.js + TypeScript

⸻

1. 项目目标

Delphi Doc MCP 的目标是构建一个 可被 AI Agent 调用的文档中间层（MCP Server），能力对齐并扩展 Apple Doc MCP：
	•	自动抓取并解析 官方 Delphi 文档
	•	Embarcadero RAD Studio VCL（DocWiki）
	•	DevExpress VCL（docs.devexpress.com）
	•	将 HTML 文档结构化为 AI 友好数据模型
	•	提供 搜索 / 索引 / 跳转 / 文档获取 能力
	•	支持 MCP 标准接口，供 LLM 工具调用

⸻

2. 整体架构概览

+----------------------+
|   AI Agent / LLM     |
|  (Claude / GPT etc.) |
+----------+-----------+
           |
           | MCP Protocol
           |
+----------v-----------+
|   Delphi Doc MCP     |
|   (Node.js + TS)     |
+----------+-----------+
           |
           +-----------------------------+
           |                             |
+----------v----------+      +-----------v-----------+
|   Search / Index    |      |   Documentation API   |
|   (Lunr / Flex)     |      |   (Markdown Output)  |
+----------+----------+      +-----------+-----------+
           |                             |
           +-------------+---------------+
                         |
               +---------v---------+
               |   Local Doc Store |
               | (JSON / Markdown)|
               +---------+---------+
                         |
        +----------------+----------------+
        |                                 |
+-------v--------+                +-------v--------+
| DevExpress     |                | Embarcadero    |
| Doc Crawler    |                | DocWiki Crawler|
+----------------+                +----------------+


⸻

3. 模块划分

3.1 Crawler（文档抓取层）

职责
	•	自动遍历官方文档站点
	•	下载类、单元、成员页面 HTML
	•	控制抓取速率，支持缓存

数据来源

文档	来源
RAD Studio VCL	https://docwiki.embarcadero.com
DevExpress VCL	https://docs.devexpress.com/VCL

实现建议
	•	axios / fetch：页面请求
	•	cheerio：HTML 静态解析
	•	puppeteer（可选）：处理 JS 渲染页面
	•	本地缓存 .cache/html/

⸻

3.2 Parser（文档解析层）

职责
	•	将 HTML 转换为结构化文档模型
	•	提取：
	•	类声明
	•	继承链
	•	描述（Remarks / Description）
	•	成员（Properties / Methods / Events / Fields）

示例：DevExpress 类声明

TcxGrid = class(TcxCustomGrid)

示例：VCL 类声明

TForm = class(TCustomForm)


⸻

3.3 文档模型（Document Model）

所有文档最终会被统一成 AI 友好结构。

ClassDoc（示例）

interface ClassDoc {
  source: 'vcl' | 'devexpress'
  unit: string
  name: string
  declaration: string
  inheritance: string[]
  description: string
  members: {
    constructors?: MemberDoc[]
    properties?: MemberDoc[]
    methods?: MemberDoc[]
    events?: MemberDoc[]
    fields?: MemberDoc[]
  }
}

MemberDoc

interface MemberDoc {
  name: string
  visibility?: string
  type?: string
  params?: string
  description: string
}


⸻

3.4 Local Doc Store（本地存储）

存储格式
	•	JSON：结构化数据
	•	Markdown：直接返回给 AI 的文档内容

目录结构建议

docs/
 ├─ vcl/
 │   ├─ Vcl.Forms/
 │   │   └─ TForm.json
 │   └─ Vcl.Controls/
 └─ devexpress/
     ├─ cxGrid/
     │   └─ TcxGrid.json


⸻

3.5 Index & Search（索引与搜索）

职责
	•	类名 / 成员名 / 描述全文索引
	•	支持：
	•	精确搜索
	•	通配符（T*Grid）
	•	模糊搜索

推荐方案

方案	特点
Lunr.js	轻量、全文索引、通配符
FlexSearch	性能更强、适合大规模
Fuse.js	偏模糊匹配


⸻

3.6 MCP Server（AI 接口层）

对 AI Agent 暴露 MCP 工具接口。

核心工具设计

Tool	功能
list_units	列出所有 VCL / DevExpress 单元
search_symbols	搜索类 / 方法 / 属性
get_documentation	获取完整文档（Markdown）

示例：get_documentation

{
  "symbol": "Vcl.Forms.TForm"
}

返回：

# TForm

**Declaration**

TForm = class(TCustomForm)

**Description**
TForm represents a standard application window...


⸻

4. 示例：TcxGrid 文档结构

{
  "source": "devexpress",
  "unit": "cxGrid",
  "name": "TcxGrid",
  "declaration": "TcxGrid = class(TcxCustomGrid)",
  "inheritance": [
    "TObject",
    "TPersistent",
    "TComponent",
    "TControl",
    "TWinControl",
    "TcxCustomControl",
    "TcxCustomGrid",
    "TcxGrid"
  ],
  "description": "The VCL Data Grid (TcxGrid) control allows you to display data as a table...",
  "members": {
    "properties": [
      { "name": "ActiveLevel", "description": "Specifies the active root grid level." }
    ],
    "methods": [
      { "name": "BeginUpdate", "description": "Avoids excessive redraw operations." }
    ]
  }
}


⸻

5. 实现路线图（Roadmap）

Phase 1：基础能力
	•	MCP Server 框架
	•	DevExpress 类文档抓取
	•	JSON 文档模型

Phase 2：VCL 支持
	•	DocWiki 单元 / 类解析
	•	成员页（Methods / Properties / Events）

Phase 3：搜索增强
	•	全文索引
	•	模糊搜索
	•	多源合并（VCL + DevExpress）

Phase 4：体验优化
	•	Markdown 模板优化
	•	示例代码提取
	•	文档版本管理

⸻

6. 设计原则
	•	AI First：结构清晰、字段明确
	•	可扩展：未来可加入 FMX、RTL、第三方库
	•	可缓存：文档更新成本低
	•	来源可信：仅使用官方文档

⸻